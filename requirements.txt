# — Audio / Video acquisition —
yt-dlp>=2024.4.9
pydub>=0.25.1
ffmpeg-python>=0.2.0
audioop-lts>=0.2.1                # restores audioop for Python 3.13+ (pydub dependency)

# — Speech-to-Text —
faster-whisper>=1.0.0
requests>=2.31.0

# — LLM Orchestration (Ollama) —
langchain>=0.2.0
langchain-core>=0.2.0
langchain-community>=0.2.0
langchain-ollama>=0.1.0

# — RAG Pipeline —
langchain-chroma>=0.1.0
langchain-text-splitters>=0.2.0
chromadb>=0.5.0
sentence-transformers>=3.0.0
langchain-huggingface>=0.0.3
huggingface-hub>=0.23.0

# — Config —
python-dotenv>=1.0.0

# — UI —
streamlit>=1.35.0
